spring:
  application:
    name: myconfigserver
  profiles:
    active: composite
  cloud:
    config:
      server:
        prefix: /config
        bootstrap: true
        composite:
          - type: prova 